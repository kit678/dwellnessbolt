export interface Session {
  id: string;
  title: string;
  startTime: string; // or Date, depending on your data structure
  endTime: string;   // or Date, depending on your data structure
  // Add any other fields that are part of the Session type
} 